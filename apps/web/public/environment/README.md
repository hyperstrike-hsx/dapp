# Rendering environment

`bonzai-sky.hdr` is imported from the local `bonzai-desktop-dapp` rendering stack. HyperStrike converts it to a PMREM environment map at runtime for physically based weapon and architecture lighting while retaining its own scene background.
